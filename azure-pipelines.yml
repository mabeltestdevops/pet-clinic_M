pool:
  vmImage: ubuntu-latest

steps:
- task: Gradle@3
  inputs:
    gradleWrapperFile: 'gradlew'
    tasks: 'build'
    publishJUnitResults: true
    testResultsFiles: '**/TEST-*.xml'
    javaHomeOption: 'JDKVersion'
    sonarQubeRunAnalysis: false
    spotBugsAnalysis: false

- task: SonarCloudPrepare@1
  inputs:
    SonarCloud: 'sonarcloud_mabel'
    organization: 'mabeltestdevops'
    projectKey: 'mabeltestdevops_pet-clinic_M'
    projectName: 'pet-clinic_M'
    cliProjectKey: 

- task: Gradle@3
  inputs:
    gradleWrapperFile: 'gradlew'
    tasks: 'sonarqube'
    publishJUnitResults: true
    testResultsFiles: '**/TEST-*.xml'
    javaHomeOption: 'JDKVersion'
    sonarQubeRunAnalysis: true
    sqGradlePluginVersionChoice: 'specify'
    sonarQubeGradlePluginVersion: '3.3'
    spotBugsAnalysis: false
- task: dependency-check-build-task@6
  inputs:
    projectName: 'pet-clinic_M'
    scanPath: '$(Build.SourcesDirectory)/build/libs'
    format: 'HTML'
- task: DockerInstaller@0
  inputs:
    dockerVersion: '17.09.0-ce'

- task: Docker@2
  inputs:
    containerRegistry: 'docker'
    command: 'login'

- task: Docker@2
  inputs:
    containerRegistry: 'docker'
    repository: 'mabelmellado1974 / pet_clinic_container'
    command: 'build'
    Dockerfile: '**/Dockerfile'
    tags: 'latest'

- script: | 
    sudo apt-get install -y rpm
    wget https://github.com/aquasecurity/trivy/releases/download/v0.27.1/trivy_0.27.1_Linux-64bit.deb
    sudo dpkg -i trivy_0.27.1_Linux-64bit.deb
    trivy image mabelmellado1974/pet_clinic_container:latest
  displayName: Image Scan
- task: Docker@2
  inputs:
    containerRegistry: 'docker'
    repository: 'mabelmellado1974 / pet_clinic_container'
    command: 'push'
    tags: 'latest'